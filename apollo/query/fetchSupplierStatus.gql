# query ($uid: String!) {
#   supplier_by_pk(id: $uid) {
#     status
#     reviewed_reason
#   }
# }

query ($uid: String!) {
  supplier_by_pk(id: $uid) {
    userById {
      status
      reason
    }
  }
}
